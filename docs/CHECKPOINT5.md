# Integration checkpoint 5

ПК1, 23.09.2026. Ветка `integration/checkpoint-5`, основа `707dde8`.

- CORE5: `97dae5d` — резервирование конкретных точных соответствий при повторении обязанностей.
- PRODUCT6: `e9c333c` — независимое подтверждение исправления владельца, проверки прав, запретов и соседних функций через настоящий backend и UI.
- Объединённый код: `10361b3`. Конфликтов нет. `main` не изменён.
- PRODUCT7: `7cc3196`, объединён документационным merge `c64b79d` — уточнённые инструкции и техническая репетиция deterministic/offline-показа. Исходники и тесты приложения не изменены относительно проверенной версии.

## Проверки объединённой версии

| Проверка | Результат |
| --- | --- |
| Все backend-тесты, включая локальные DOCX организатора | 94 passed |
| Все frontend unit-тесты | 79 passed |
| Frontend production build | PASS |
| Все браузерные тесты | 23 passed |
| Новый настоящий Astra через production UI на документах организатора | PASS, полный сценарий 79,3 секунды |
| Источники нового семантического результата | 1760 вхождений сверены с исходными DOCX |
| Python compile, `pip check`, `git diff --check` | PASS |
| Health напрямую на 8000 и через frontend 5173 | PASS |
| Исключение локальных DOCX, `.env` и артефактов из Git; отсутствие ключа в frontend bundle | PASS |

Обычные backend и браузерные тесты выполняются без платного вызова модели. Живой Astra запускается отдельно через HTTP upload из production UI. Для браузерной батареи используются изолированные порты 8002 и 5185–5187; приложение пользователя остаётся на 5173.

## Содержание изменения

Повторяющееся назначение у одного владельца больше не расходует точное соответствие другого. Перед приблизительным сравнением CORE5 резервирует каждую запись AFTER не более одного раза: сначала совпадение текста, вида и владельца, затем оставшиеся точные совпадения текста и вида. Четыре новые проверки воспроизводили ошибку до исправления; три из них проверяют все 12 комбинаций порядка входных записей. См. [CORE5_HANDOFF.md](CORE5_HANDOFF.md).

Оставшийся без отдельного соответствия повтор всё ещё может быть кандидатом `LOST` с пояснением о возможной консолидации. Это не доказательство исчезновения функции из организации. На исходных документах организатора все 450 извлечённых записей, преобразования, сопоставления, findings и summary deterministic-анализа совпали с CORE4.

PRODUCT6 закрепляет сохранённую функцию контроля `5.2.1 → 5.2.1`, правильных владельцев, статусы и обе цитаты в браузере. Дополнительные сценарии подтверждают виды `RIGHT` / `PROHIBITION`, соседнюю `FUNCTION` и отсутствие ложного исполнения в проверенных формулировках. Историческая ошибка и повторная независимая проверка разделены в [PRODUCT5_REVIEW.md](PRODUCT5_REVIEW.md).

## Демонстрация и сдача

Новый настоящий запрос на редакциях 8 и 9 выполнен 23.09.2026 в 16:20–16:22 (UTC+5) на объединённом коде `10361b3`, конфигурация модели `gpt-6-astra`. Анализ `998420a6ab280ece110a03ae`: 450 извлечённых записей, 57 кандидатов findings, аналитическая записка с 14 источниками. Все 1760 вхождений evidence сверены по документу, ID, номеру пункта и исходному тексту. Извлечённые объекты совпадают с deterministic-разбором; явные владельцы контрольных подпунктов сохранены. Все findings и записка остаются `NEEDS_REVIEW`.

В браузере проверены режим, граф 3 → 5, таблица функций, точная цитата, записка со статусом и источниками, скачанный Markdown; ошибок JavaScript нет. Сохранены JSON, Markdown, три снимка, `provenance.json` и `verification.json` в ignored `artifacts/prepared-semantic-checkpoint5/`. Материалы checkpoint 3 сохранены отдельно и не перезаписаны. Количество выводов и время относятся к этому запуску, не к гарантированной скорости или точности.

Автоматизированная браузерная сессия после проверки закрыта. Снимки и Markdown доступны для резервного показа; результат не импортируется обратно в UI. Для интерактивной защиты нужно заранее выполнить анализ в пользовательской вкладке и оставить её открытой. Локальная памятка к новому результату: `artifacts/prepared-semantic-checkpoint5/REHEARSAL.md`.

Текст формы: [SUBMISSION.md](../SUBMISSION.md). Сценарий выступления: [DEMO.md](../DEMO.md), резерв: [DEMO_BACKUP.md](../DEMO_BACKUP.md), ответы жюри: [JUDGES_QA.md](../JUDGES_QA.md).

PRODUCT7 принят после проверки diff и [отчёта ПК2](DEMO_REHEARSAL_REVIEW.md). На базе `e9c333c` ПК2 прошёл настоящий deterministic-путь за 63,667 секунды и отдельный offline DEMO за 67,182 секунды. Оба замера включают 60 секунд запланированных пауз; устная речь не измерялась. Backend во время offline-прохода был остановлен, API-запросов не было. Источники, навигация и скачивание Markdown проверены. Эти результаты относятся к технической репетиции ПК2; semantic-режим в PRODUCT7 не запускался. Повторный полный прогон тестов после изменений только Markdown не выполнялся.

PDF/Excel/OCR, производственная аутентификация и постоянное хранение результатов не реализованы. Все интерпретационные выводы и записка требуют экспертной проверки. Проверенные сценарии и совпадение цитат не являются измерением общей точности. Финальный merge в `main` и отправка формы на платформе ещё не выполнены.
