# Integration checkpoint 3

Проверено на ПК1 23.09.2026. Ветка `integration/checkpoint-3`; база `f222c5e`.

- CORE3: `ad716e1` — корректные владельцы функций и обработка пустых предложений модели.
- PRODUCT3: `92c00fb` — понятные ошибки Astra, сценарии выступления и резервного показа.
- Merge: `a5dd8f2`. Конфликтов нет. `main` не изменён.

## Проверки интегрированного состояния

| Проверка | Результат |
| --- | --- |
| Все backend-тесты, включая локальные DOCX организатора | 71 passed |
| Все frontend unit-тесты | 79 passed |
| Все браузерные тесты demo/API/real deterministic | 18 passed |
| Frontend production build | PASS |
| Отдельный живой Astra через production UI на документах организатора | PASS, 93,7 секунды |
| Источники живого результата | 1793 вхождения сверены с оригиналами |
| Python compile, `pip check`, `git diff --check` | PASS |
| Исключение DOCX, `.env`, артефактов из Git; отсутствие локального ключа в frontend bundle | PASS |

Браузерная батарея выполнялась с собственным backend на 8002 и frontend на 5185–5187: изменены только тестовые порты/proxy, все 18 тестов выполнены без изменения проверок. Это позволило оставить пользовательский адрес 5173 доступным. Обычная команда из README остаётся `npm run test:e2e` при свободных стандартных тестовых портах.

Локальный временный checkout CORE3 вынесен за пределы репозитория, чтобы стандартные `pytest` и `npm test` не собирали вторые копии тестов. Итоговые количества выше относятся только к интеграционной ветке.

## Закрытые дефекты и содержательная проверка

P2: пустой обязательный текст отдельного предложения модели больше не ломает весь результат frontend. Предложение отклоняется с предупреждением; исходный кандидат и остальные корректные результаты сохраняются. Проверены пустые строки и пробелы, настоящий SDK с HTTP mock, FastAPI и frontend-валидатор.

P3: frontend различает ошибки авторизации, недоступной модели, квоты и таймаута. Браузерные проверки подтверждают безопасное сообщение, отсутствие автоматической подмены LIVE на demo, явный fallback, reset и успешный новый запрос.

На документах организатора исправлены два лишних назначения функций: теперь 450 записей вместо 452. Контрольные подпункты `5.3.2.а` и `5.3.2.б` относятся к явно указанным владельцам; остальные записи сохранены.

Синтетические DOCX с заранее объявленными изменениями проверены через настоящий Astra: новое подразделение, переименование, перенос, отсутствие функции, дублирование, потенциальный конфликт и источники. Самостоятельный HTTP-запуск занял 40,3 секунды; отдельный полный production browser-поток — 33,1 секунды. Это проверки конкретных сценариев, не измерение общей точности. Подробности и команды: [CORE3_HANDOFF.md](CORE3_HANDOFF.md).

## Материалы выступления

- [DEMO.md](../DEMO.md): показ заранее полученного реального semantic-результата за 2–3 минуты.
- [DEMO_BACKUP.md](../DEMO_BACKUP.md): сохранённый результат, deterministic backend и отдельный синтетический offline demo.
- [JUDGES_QA.md](../JUDGES_QA.md): ответы о модели, источниках, статусах, данных и ограничениях.

Локальные резервные материалы реального запуска сохраняются в игнорируемом `artifacts/prepared-semantic/`. Происхождение и время фиксируются в `provenance.json`; сохранённые файлы не выдаются за новый запрос. Интерактивный результат хранится только в открытой вкладке, импорта сохранённого JSON в интерфейс нет.

На интегрированной версии выполнен новый настоящий браузерный запрос к Astra с редакциями 8 и 9. Получено 63 findings и записка с 18 источниками; все 450 извлечённых записей совпали с детерминированным разбором, в том числе исправленные владельцы контрольных подпунктов. Проверены граф, исходная цитата, записка, её статус и источники, скачанный Markdown. Ошибок JavaScript нет. Сохранены снимки обзора, источников и заключения, Markdown, JSON результата, `provenance.json` и `verification.json`. Анализ: `c34f9a34864f963ec30ba53d`; количество findings относится только к этому запуску.

## Перед финальной сдачей

Нужно проверить итоговую инструкцию запуска и текст формы сдачи, согласовать финальный merge в `main`, выполнить итоговый push и отдельно нажать «Сдать решение» на платформе. PDF/Excel/OCR и производственный контур доступа не реализованы. Семантические findings и записка требуют экспертной проверки; цитата подтверждает источник, но не правильность интерпретации.
